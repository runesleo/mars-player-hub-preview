import{loadJSON,esc}from"./common.mjs";const rows=await loadJSON("./data/updates.json"),box=document.querySelector("#updates-list");box.innerHTML=rows.map(x=>'<article class="card update-card"><div class="sourcebar"><span class="badge '+(x.status==="verified-first-party"?"good":"warn")+'">'+esc(x.status)+'</span><span>'+esc(x.date)+'</span><a target="_blank" rel="noreferrer" href="'+esc(x.sourceUrl)+'">'+esc(x.source)+'</a></div><h2>'+esc(x.title)+'</h2><div class="flow"><div><b>What changed</b><span>'+esc(x.whatChanged)+'</span></div><div><b>So what</b><span>'+esc(x.soWhat)+'</span></div><div><b>What to check</b><span>'+esc(x.whatToCheck)+'</span></div></div></article>').join("");